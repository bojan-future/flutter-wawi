///used for code generation on script-side
class FutureTableNumber {
  ///
  const FutureTableNumber(int table);
}

///used for code generation on script-side
class FutureColumnNumber {
  ///
  const FutureColumnNumber(int column);
}
